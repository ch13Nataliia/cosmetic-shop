import { createTheme } from "@mui/material/styles";

let theme = createTheme({
  palette: {
    primary: {
      main: '#fff3e0',
    },
    secondary: {
      main: '#aeea00',
    },
  },
});

export default theme;